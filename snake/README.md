Train a new agent by running **`train_agent.py`**, or load a pretrained model by simply running **`play.py`**.
The current model was only trained for 200 episodes as a demo and achieves an average score of 15 points.
